# This file contains MbKit's requirements
#
# These can be installed using the following
# command:
#       pip install -r requirements.txt

setuptools
# Require this to be Python2.6 compatible for now
biopython ==1.66

